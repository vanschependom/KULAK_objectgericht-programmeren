package model.oefening2.card;/** * A class of numbered cards used in a game of Thieves. * * @invar  The value of a numbered card ranges from 1 to 10. *         | 1 <= getValue() <= 10 */public class NumberedCard extends Card {    /**     * The value of this numbered card.     */    private final int value;    /**     * Initialize a new numbered card with given value.     *     * @param     value     *            The initial value for the new numbered card.     * @pre       The initial value for this card must be between     *            1 and 10.     *            | 1 <= value <= 10     * @effect    The new card is initialized with the given value.     *            | getValue() = value     */    public NumberedCard(int value) {        this.value = value;    }    /**     * Return the value of this numbered card.     */    public int getValue() {        return value;    }    /**     * Check whether this numbered card matches the other card.     *     * @param   other     *          The other card upon which this card must match.     * @return  True if the other card matches this numbered card.     *          | other.matchesOnNumbered(this)     */    public boolean matchesOn(Card other) {        return other.matchesOnNumbered(this);    }    /**     * Check whether this numbered card matches the other numbered     * card.     *     * @param   other     *          The other numbered card upon which this card must     *          match.     * @return  True if the value of the other card is one higher     *          or lower than this numbered card.     *          | abs(getValue() - other.getValue()) = 1     */    public boolean matchesOnNumbered(NumberedCard other) {        return Math.abs(getValue() - other.getValue()) == 1;    }    /**     * Check whether this numbered card matches the given     * jack card.     *     * @param   other     *          The other jack card upon which this card     *          must match.     * @return  True if the value of this numbered card is 10.     *          | getValue() = 10     */    public boolean matchesOnJack(JackCard other) {        return getValue() == 10;    }}