package model.event;


/**
 * An enum representing the categories an event can belong to.
 * For now, we distinguish academic events and recreational events.
 */
public enum EventCategory {
    ACADEMIC,
    RECREATIONAL
}
